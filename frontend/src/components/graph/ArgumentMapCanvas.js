/**
 * ArgumentMapCanvas — DEPRECATED
 *
 * This file is kept as a re-export shim so any lingering import doesn't break
 * while the codebase migrates. The real implementation is in BoardRouter.
 *
 * MapDetailPage now imports BoardRouter directly.
 */
export { default } from '../boards/BoardRouter';
