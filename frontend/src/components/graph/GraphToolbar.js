/**
 * GraphToolbar — DEPRECATED
 *
 * The toolbar is now built inline inside FreeformBoard and DebateBoard.
 * This shim prevents any lingering import from breaking.
 */
export default function GraphToolbar() { return null; }
